class MyRoutes {
  ///here we have done static memeber variable so we need not create n object everytime like x() dirsectly use it
  static String loginRoute = "/login";
  static String homeRoute = "/home";
}
